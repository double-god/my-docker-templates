# 阿里云 Access Key (从 RAM 控制台获取)
ALIBABA_CLOUD_ACCESS_KEY_ID=你的真实AccessKeyID
ALIBABA_CLOUD_ACCESS_KEY_SECRET=你的真实AccessKeySecret

# 阿里云 OSS 配置
OSS_BUCKET_NAME=doublegooood-exam-bucket # 你的 Bucket 名称
OSS_REGION=oss-cn-hangzhou            # 你的 Bucket 地域 ID (不是 Endpoint)
OSS_ENDPOINT=oss-cn-hangzhou.aliyuncs.com # 你的 Bucket Endpoint

# 阿里云 STS 配置
STS_ENDPOINT=sts.cn-hangzhou.aliyuncs.com # 你的 STS Endpoint

# (可选，推荐) RAM 角色 ARN，如果使用的话
# ALIBABA_CLOUD_RAM_ROLE_ARN=acs:ram::你的账号ID:role/你的角色名